package(default_visibility = ["//visibility:public"])

load("@build_bazel_rules_nodejs//:defs.bzl", "nodejs_binary")
load("@npm_bazel_typescript//:index.bzl", "ts_library")

ts_library(
    name = "build",
    srcs = glob(["*.ts"]),
)

nodejs_binary(
    name = "main",
    entry_point = "bazel_demo/src",
    data = [":build"],
)
